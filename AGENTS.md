# Veil 项目约定

- 工作流程：用户提出需求 → 整理范围与交互 → 用户明确确认 → 编码。已确认任务内的实现、修复和验证继续完成，无需反复确认。
- 唯一本地预览地址为 http://localhost:5106/。禁止自动换端口；占用时先排查。除非用户明确要求，否则不可修改。
- 当前阶段仅本地保存，不接入外部服务器、数据库、统计或账号服务，不对外部署。
- 永远优先可维护性；不得为了速度将代码塞入不相关模块或留下“以后再整理”的临时代码。
- src/features 按业务划分；业务规则、UI、手势、数据和存储各司其职。公共组件放 shared，外部能力放 infrastructure。
- 图片、图标统一在 public/assets 对应目录，原始牌面不做生成式替代。新增资源记录来源。
- 页面不得直接读写 localStorage；经统一仓储接口处理数据校验、版本与失败提示。确认抽牌与记录必须原子保存。
- 所有随机使用 Web Crypto，整数采用拒绝采样，洗牌采用 Fisher–Yates。禁止 Math.random 抽牌、暗中加权或预定结果。
- 洗牌仅重排剩余牌，不回收桌面牌、不重开或归档当前轮次。
- 样式设计令牌集中维护，功能 CSS 就近维护。减少重复，避免不必要的框架或抽象。
- 每次修改同步 docs/PROJECT.md，记录功能、架构、验证、限制及后续事项。
- 完成改动后执行对应检查；核心随机、持久化与状态转移需要测试，交互修改需检查桌面和手机布局。
